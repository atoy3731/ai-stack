defaultNamespace: ollama
helm:
  repo: https://otwld.github.io/ollama-helm/
  version: 1.53.0
  releaseName: ollama
  chart: ollama
  valuesFiles:
  - ./values.yaml

labels:
  app: ollama

# dependsOn:
# - selector:
#     matchLabels:
#       app: nvidia-operator
